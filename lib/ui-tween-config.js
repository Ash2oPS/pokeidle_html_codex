export const UI_TWEEN_MODAL_OPEN = Object.freeze({
  overlayDurationMs: 180,
  panelDurationMs: 290,
  panelOffsetY: 26,
  panelStartScale: 0.94,
  panelStartBlur: 3.2,
});
export const UI_TWEEN_MODAL_CLOSE = Object.freeze({
  overlayDurationMs: 160,
  panelDurationMs: 220,
  panelOffsetY: 18,
  panelEndScale: 0.965,
  panelEndBlur: 2.5,
});
export const UI_TWEEN_POPUP_OPEN = Object.freeze({
  durationMs: 200,
  offsetY: 12,
  startScale: 0.95,
  startBlur: 2.1,
});
export const UI_TWEEN_POPUP_CLOSE = Object.freeze({
  durationMs: 150,
  offsetY: 10,
  endScale: 0.97,
  endBlur: 1.8,
});
