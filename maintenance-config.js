// Maitre: `enabled: true` active la maintenance sur le web de prod.
// Laisse `message` vide pour reutiliser le message par defaut.
export const MAINTENANCE_CONFIG = Object.freeze({
  enabled: true,
  message: "Le jeu est en maintenance. À la semaine prochaine. :)",
});
